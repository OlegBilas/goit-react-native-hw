import { StyleSheet } from "react-native";

export const commonStyles = StyleSheet.create({
  fonts: { fontFamily: "Roboto-400", fontSize: 16, lineHeight: 18.75 },
  styleVariables: { accentColor: "#FF6C00" },
});
